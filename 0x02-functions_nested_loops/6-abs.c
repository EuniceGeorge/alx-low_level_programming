#include "main.h"

/**
 * _abs- The entry point
 *@n: the value to te
 *Return: the absolute value
 */

int _abs(int n)
{
	int abs_value;

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

