#include <stdio.h>

/**
 * main- This is the main function
 * Return: 0 on success
 */

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
